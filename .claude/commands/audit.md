Run the following steps in order:

1. Run `npm audit` to check for vulnerable packages
2. Run `npm audit fix` to fix them automatically  
3. Run `npm test` to verify nothing broke

Report a summary of what was fixed and any remaining issues.
